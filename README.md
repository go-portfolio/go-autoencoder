# Go Neural Autoencoder

**Go Neural Autoencoder** — это простой автоэнкодер, реализованный на языке Go, с нуля, без внешних библиотек для машинного обучения. Проект демонстрирует работу нейронной сети для сжатия и восстановления бинарных данных, а также подходы к обратному распространению ошибки и обновлению весов.

---

## 📌 Описание проекта

Автоэнкодер — это тип нейронной сети, который обучается кодировать входные данные в более компактное представление (латентное пространство) и восстанавливать их обратно. В данном проекте:

* Входной вектор: бинарные данные длиной `inputSize`.
* Скрытый слой (латентное пространство): `latentSize` нейронов.
* Используется **сигмоида** как функция активации.
* Обучение происходит с помощью **собственной реализации SGD** и обратного распространения ошибки.
* Поддержка батчей данных, тестирование на данных, которых модель не видела во время обучения.
* Возможность сохранения и загрузки обученной модели.

---

## ⚙️ Практическая ценность

1. **Сжатие данных**: автоэнкодер может сокращать размерность бинарных векторов, что полезно для хранения или передачи данных.
2. **Выявление паттернов**: даже на случайных данных автоэнкодер может выявлять повторяющиеся структуры.
3. **Обучение без библиотек ML**: позволяет понять внутреннюю работу нейронной сети и алгоритма обратного распространения ошибки (backpropagation).
4. **Основы для более сложных проектов**: этот проект можно расширить:

   * Добавлением более сложных функций активации (ReLU, tanh),
   * Поддержкой непрерывных данных,
   * Параллельными вычислениями на CPU для ускорения обучения.

---

## 🛠️ Использование

1. Клонируйте репозиторий:

```bash
git clone https://github.com/go-portfolio/go-neuro-autoencoder.git
cd go-neuro-autoencoder
```

2. Запустите проект:

```bash
go run main.go
```

3. Пример вывода:

```
Epoch 0 Loss: 0.258763
Epoch 200 Loss: 0.013245
...
=== Reconstruction on training data ===
Train Sample 0:
Input:        [0 1 0 1 1 0 1 0]
Reconstructed: [0 1 0 1 1 0 1 0]
...
=== Reconstruction on test data ===
Test Sample 0:
Input:        [1 0 0 1 0 1 0 1]
Reconstructed: [1 0 0 1 0 1 0 1]
```

---

## 📊 Настройки

В `main.go` можно изменять:

* `inputSize` — длина входного вектора.
* `latentSize` — размер скрытого слоя.
* `numTrain` и `numTest` — количество тренировочных и тестовых примеров.
* `epochs` — количество эпох обучения.
* `learningRate` — скорость обучения.

---

## 🔍 Структура проекта

```
/internal
    /autoencoder   - реализация автоэнкодера
    /mathutils     - матричные операции и функции активации
main.go            - скрипт генерации данных, обучения и тестирования
```

---

## ⚡ Возможные улучшения

* **Распараллеливание вычислений** для ускорения обучения (например, обновление весов и вычисление градиентов).
* **Добавление поддержки разных функций активации** (например, ReLU, tanh).
* **Работа с реальными данными** (например, бинаризованными изображениями или текстом).
* **Сохранение и загрузка весов модели** для продолжения обучения или использования модели в других приложениях.
* **Использование различных оптимизаторов** для улучшения сходимости (например, Adam или RMSprop).

---

## 💡 Заключение

Проект полезен как учебный пример и как прототип для небольших задач сжатия данных или feature extraction. Он показывает базовые принципы работы нейронных сетей и демонстрирует практическую ценность автоэнкодеров. Проект можно легко модифицировать и расширять для работы с реальными данными и более сложными архитектурами нейронных сетей.

